---
# inventories/master/group_vars/all.yml
# Master-Server configuration

admin_user: "srvadmin"
admin_ssh_pubkeys: []  # Add your SSH public key(s) here

is_lxc: true
docker_install: true
server_roles: [master]

# Master-specific: admin services bind to localhost, Caddy terminates TLS
# Ports defined in roles (pocketid: 1411, tinyauth: 9090, vaultwarden: 8222, semaphore: 3000, grafana: 3001, baserow: 8085)
