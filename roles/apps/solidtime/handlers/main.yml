---
- name: restart caddy
  ansible.builtin.command: docker restart caddy
  changed_when: true

- name: restart solidtime
  community.docker.docker_compose_v2:
    project_src: "{{ solidtime_stack_path }}"
    state: restarted
