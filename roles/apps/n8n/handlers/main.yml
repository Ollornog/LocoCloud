---
- name: restart caddy
  ansible.builtin.command: docker restart caddy
  changed_when: true

- name: restart n8n
  community.docker.docker_compose_v2:
    project_src: "{{ n8n_stack_path }}"
    state: restarted
