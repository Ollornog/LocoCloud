---
- name: restart caddy
  ansible.builtin.command: docker restart caddy
  changed_when: true

- name: restart easy-appointments
  community.docker.docker_compose_v2:
    project_src: "{{ easy_appointments_stack_path }}"
    state: restarted
