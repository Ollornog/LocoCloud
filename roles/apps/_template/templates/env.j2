# GENERIERT DURCH ANSIBLE — NICHT MANUELL EDITIEREN
# TEMPLATE — Replace with your app's environment variables
APP_URL=https://{{ myapp_domain }}
{% if myapp_oidc_enabled and myapp_oidc_client_id | length > 0 %}
# OIDC Configuration
OIDC_ENABLED=true
OIDC_ISSUER={{ myapp_oidc_provider_url }}
OIDC_CLIENT_ID={{ myapp_oidc_client_id }}
OIDC_CLIENT_SECRET={{ myapp_oidc_client_secret }}
{% endif %}
