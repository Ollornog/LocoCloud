---
# roles/apps/calcom/tasks/main.yml

- name: Deploy Cal.com
  ansible.builtin.import_tasks: deploy.yml
  tags: [calcom, deploy]

- name: Configure OIDC for Cal.com
  ansible.builtin.import_tasks: oidc.yml
  tags: [calcom, oidc]
  when: calcom_oidc_enabled | default(false)
