---
- name: Deploy Zulip
  ansible.builtin.import_tasks: deploy.yml
  tags: [zulip, deploy]

- name: Configure OIDC for Zulip
  ansible.builtin.import_tasks: oidc.yml
  tags: [zulip, oidc]
  when: zulip_oidc_enabled | default(false)
