---
# roles/apps/vaultwarden/tasks/main.yml

- name: Deploy Vaultwarden
  ansible.builtin.import_tasks: deploy.yml
  tags: [vaultwarden, deploy]

- name: Configure Vaultwarden SSO via PocketID
  ansible.builtin.include_tasks: oidc.yml
  when: pocketid_api_token | default('') | length > 0
  tags: [vaultwarden, oidc]
