# GENERIERT DURCH ANSIBLE — NICHT MANUELL EDITIEREN
# AppFlowy-Cloud shared .env (used by gotrue, appflowy-cloud, worker)

# --- Base URLs ---
FQDN={{ appflowy_domain }}
SCHEME=https
WS_SCHEME=wss
APPFLOWY_BASE_URL=https://{{ appflowy_domain }}
APPFLOWY_WEBSOCKET_BASE_URL=wss://{{ appflowy_domain }}/ws/v1

# --- Logging ---
RUST_LOG=info
