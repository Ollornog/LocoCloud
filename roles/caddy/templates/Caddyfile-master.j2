# ============================================
# GENERIERT DURCH ANSIBLE — NICHT MANUELL EDITIEREN
# Master-Server Admin-Dienste
# Generiert: {{ ansible_facts.date_time.iso8601 }}
# ============================================

{% set tls_mode = loco.admin.tls_mode | default('acme') %}
{% if tls_mode == 'internal' %}
{
	pki {
		ca local {
			name "LocoCloud Internal CA"
		}
	}
}
{% endif %}

{% if tls_mode == 'internal' %}
(tls_config) {
	tls internal
}
{% elif tls_mode == 'dns' %}
(tls_config) {
	tls {
		dns {{ loco.admin.dns_provider }} {env.DNS_API_TOKEN}
	}
}
{% else %}
(tls_config) {
}
{% endif %}

(public) {
	header -Server
	header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
	header X-Content-Type-Options "nosniff"
	header X-Frame-Options "SAMEORIGIN"
	header Referrer-Policy "strict-origin-when-cross-origin"
	header Permissions-Policy "camera=(), microphone=(), geolocation=()"
}

{% if loco.tinyauth.enabled | default(false) %}
(auth) {
	forward_auth 127.0.0.1:{{ tinyauth_port | default(9090) }} {
		uri /api/auth/caddy
	}
}
{% endif %}

# --- PocketID (Admin OIDC Provider) ---
{{ loco.urls.pocketid }} {
	import tls_config
{% if tls_mode == 'cert_sync' %}
	tls /opt/certs/id.admin-fullchain.pem /opt/certs/id.admin-privkey.pem
{% endif %}
	import public
	@blocked path /register*
	handle @blocked {
		respond "Access Denied" 403
	}
	handle {
		reverse_proxy 127.0.0.1:{{ pocketid_port }}
	}
}

{% if loco.tinyauth.enabled | default(false) %}
# --- Tinyauth (Forward Auth Portal) ---
auth.{{ loco.admin.full_domain }} {
	import tls_config
{% if tls_mode == 'cert_sync' %}
	tls /opt/certs/auth.admin-fullchain.pem /opt/certs/auth.admin-privkey.pem
{% endif %}
	import public
	reverse_proxy 127.0.0.1:{{ tinyauth_port | default(9090) }}
}
{% endif %}

# --- Vaultwarden (Admin Credential Store) ---
{{ loco.urls.vaultwarden }} {
	import tls_config
{% if tls_mode == 'cert_sync' %}
	tls /opt/certs/vault.admin-fullchain.pem /opt/certs/vault.admin-privkey.pem
{% endif %}
	import public
	@block_admin path /admin*
	handle @block_admin {
		respond "Access Denied" 403
	}
	handle {
		reverse_proxy 127.0.0.1:{{ vaultwarden_port }}
	}
}

# --- Semaphore (Ansible UI) ---
{{ loco.urls.semaphore }} {
	import tls_config
{% if tls_mode == 'cert_sync' %}
	tls /opt/certs/deploy.admin-fullchain.pem /opt/certs/deploy.admin-privkey.pem
{% endif %}
	import public
	reverse_proxy 127.0.0.1:{{ semaphore_port }}
}

# --- Grafana (Monitoring) ---
{{ loco.urls.grafana }} {
	import tls_config
{% if tls_mode == 'cert_sync' %}
	tls /opt/certs/grafana.admin-fullchain.pem /opt/certs/grafana.admin-privkey.pem
{% endif %}
	import public
	reverse_proxy 127.0.0.1:{{ grafana_port }}
}

# --- NocoDB (Berechtigungskonzept) ---
{{ loco.urls.nocodb }} {
	import tls_config
{% if tls_mode == 'cert_sync' %}
	tls /opt/certs/nocodb.admin-fullchain.pem /opt/certs/nocodb.admin-privkey.pem
{% endif %}
	import public
	@blocked path /signup*
	handle @blocked {
		respond "Access Denied" 403
	}
	handle {
		reverse_proxy 127.0.0.1:{{ nocodb_port }}
	}
}

# --- Catch-All ---
:80, :443 {
	respond "Access Denied" 403
	header -Server
}
