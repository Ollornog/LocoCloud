---
# roles/watchtower/tasks/main.yml
# Watchtower is DEPRECATED — replaced by Ansible updates via Semaphore.
# This role now removes existing Watchtower installations idempotently.

- name: Remove Watchtower (deprecated — updates via Ansible/Semaphore)
  ansible.builtin.import_tasks: remove.yml
  tags: [watchtower, remove]
