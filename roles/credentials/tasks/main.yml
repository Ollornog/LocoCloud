---
# roles/credentials/tasks/main.yml
# Dispatcher — use specific tasks via include_role + tasks_from

- name: Store credential in Vaultwarden
  ansible.builtin.import_tasks: store.yml
  tags: [credentials, store]
