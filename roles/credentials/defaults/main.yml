---
# roles/credentials/defaults/main.yml
# Credential storage — either via bw CLI or local pending file.
# This role is included by other roles to persist secrets.

# Local file for credentials pending import into Vaultwarden.
# Used during bootstrap when bw CLI is not yet configured.
credentials_pending_file: "/root/.loco-credentials-pending.json"

# Bitwarden CLI data directory
bw_appdata_dir: "/root/.bw"

# Credential to store (set by calling role)
credential_name: ""
credential_username: ""
credential_password: ""
credential_uri: ""
credential_notes: ""
credential_folder_name: ""
