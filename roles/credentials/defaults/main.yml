---
# roles/credentials/defaults/main.yml
# Vaultwarden API integration — store and retrieve credentials
# This role is included by other roles to persist secrets.

# Vaultwarden API URL (from global config: loco.vaultwarden.url)
vw_api_url: ""

# Vaultwarden API token path on master
vw_api_token_path: "/root/.loco-vaultwarden-token"

# Organization ID in Vaultwarden
vw_organization_id: ""

# Credential to store (set by calling role)
credential_name: ""
credential_username: ""
credential_password: ""
credential_uri: ""
credential_notes: ""
credential_folder_id: ""
